/**
 * Find all html files in the project
 *
 * @param base
 * @param extension
 * @param files
 * @param result
 * @returns
 */
export default function findHtmlFunction(base: string, extension?: string, files?: string[], result?: string[]): string[];
