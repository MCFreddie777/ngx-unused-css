import { SCSSCompilerConfig } from '../../types/config.type';
/**
 * Compile SCSS
 * @param {string} cssPath
 * @param config
 */
export default function compileScss(cssPath: string, config: SCSSCompilerConfig): string;
